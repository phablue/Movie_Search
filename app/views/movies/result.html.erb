<%= image_tag("film-roll", class: "search-image") %>
<div class="result">
  <p class="result-message"><%= flash[:notice] %></p>
  <ul class="list-unstyled">
    <% @movies.each do |movie| %>
      <li><%= link_to movie.title, movie_path(movie) %></li>
    <% end %>
  </ul>
</div>
